{
  "name": "feedme",
  "private": true,
  "workspaces": ["feedme.client"],
  "scripts": {
    "ng": "npm run ng --prefix feedme.client",
    "start": "npm run start --prefix feedme.client",
    "build": "npm run build --prefix feedme.client",
    "test": "npm run test --prefix feedme.client",
    "lint": "npm run lint --prefix feedme.client"
  }
}
